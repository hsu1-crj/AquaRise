# 水下垃圾检测模型评估与训练建议

## 1. 本次评估对象

- 权重：`runs/detect/underwater_baseline_v1/weights/best.pt`
- 模型：YOLO11n，22 类，输入尺寸 640
- 数据：TrashCan 1.0 Instance Version，训练/验证/测试图片数为 5048/1442/722
- 训练配置：50 epochs，预训练权重，batch=16，seed=0，`best.pt` 按验证集 mAP 保存

## 2. 实测结果

| 数据集 | Images | Instances | Precision | Recall | mAP@50 | mAP@50:95 |
|---|---:|---:|---:|---:|---:|---:|
| Val | 1442 | 2428 | 0.863 | 0.852 | 0.892 | 0.689 |
| Test | 722 | 1265 | 0.827 | 0.843 | 0.868 | 0.661 |

测试集相对验证集下降约 0.024（mAP@50）和 0.028（mAP@50:95），说明模型有一定泛化落差，但整体检测能力稳定。训练日志中第 49 轮的 mAP@50:95 最高，为 0.689；第 50 轮没有继续提升，因此当前使用 `best.pt` 是正确的。

### 测试集优先改进类别

| 类别 | Instances | mAP@50 | mAP@50:95 | 判断 |
|---|---:|---:|---:|---|
| animal_starfish | 57 | 0.685 | 0.343 | 小目标/形态变化大，定位质量差 |
| animal_crab | 34 | 0.732 | 0.480 | 外观和背景容易混淆 |
| animal_shells | 44 | 0.746 | 0.436 | 样本少，目标细碎 |
| trash_bottle | 6 | 0.684 | 0.619 | 测试样本极少，结论不稳定 |
| trash_net | 13 | 0.722 | 0.575 | 细长、缠绕、遮挡导致漏检 |
| trash_rope | 13 | 0.803 | 0.514 | 检出尚可，但框定位不够紧 |

不要仅根据 `trash_bottle` 的单次测试结果调整模型，它只有 6 个测试实例；应优先看多次随机划分或补充数据后的结果。

## 3. 一个重要的评估风险

当前转换脚本在全部图片上固定随机打乱后按 70/20/10 划分。原始图片来自视频帧，同一视频的相邻帧可能同时出现在 train、val、test，因此指标可能高于真实“新视频”表现。下一轮建议按视频 ID（文件名中的 `vid_xxxxxx`）分组后划分：同一个视频只能进入一个 split。报告中应同时保留当前结果作为基线，再新增一组按视频隔离的严格结果。

## 4. 借鉴资料后的训练建议

参考 TrashCan 论文（[arXiv:2007.08097](https://arxiv.org/pdf/2007.08097)）以及两个水下垃圾检测实现（[HarshaVanamala/Underwater-Trash-Detection](https://github.com/HarshaVanamala/Underwater-Trash-Detection)、[omprxkash/underwater-waste-detection](https://github.com/omprxkash/underwater-waste-detection)），建议遵循以下顺序：

1. **先保证划分和标签可靠**：维持固定 seed，检查空标签、越界框、重复帧和类别 ID；最终结果用从未参与调参的 test 集。论文中的实例级标注也说明，水下垃圾既要评价“有没有检出”，也要评价实例框的位置质量，不能只看分类准确率。
2. **先做可比较的基线**：固定模型、输入尺寸、epoch、增强和 seed，一次只改一个因素。记录 mAP@50、mAP@50:95、Precision、Recall、每类 AP、推理速度和权重大小。
3. **针对水下成像增强**：保留 HSV、亮度/对比度、水平翻转和适度缩放；可单独实验颜色偏移、模糊、噪声、雾化/低对比度增强。增强应模拟真实 ROV 画面，避免过强变换破坏瓶子、渔网和绳索的形状。
4. **针对长尾类别补数据**：训练实例数从 `trash_cup=40`、`trash_clothing=58`、`trash_snack_wrapper=59` 到 `rov=2321`、`trash_unknown_instance=1930` 差异很大。优先补 starfish、shells、crab、net、rope 和低频垃圾，或使用针对低频类别的 oversampling/copy-paste，并检查合成目标是否自然。
5. **针对小目标提高分辨率**：先做 640 与 800/960 的对照实验；显存不足时降低 batch，不要先牺牲输入尺寸。对小目标较多的类别，切图训练或适度增加 `imgsz` 往往比盲目增加 epochs 更直接。
6. **逐步扩大模型和训练周期**：先完成 YOLO11n 的 100~150 epochs 对照，再测试 YOLO11s。只在验证集持续提升且严格测试集同步提升时保留更大模型，避免仅因训练集 loss 下降而判断模型变好。
7. **调整推理阈值而不是只看默认值**：在验证集上画 PR 曲线，按项目目标选择 conf。若系统更怕漏掉垃圾，降低 conf 并用验证集选择阈值；若更怕误报，再提高阈值。阈值确定后一次性在 test 集报告，不能用 test 集反复调阈值。
8. **为视频场景增加时序后处理**：逐帧检测可对同一目标重复计数。上线前应按视频做抽帧统计、连续帧投票或跟踪，并单独报告 frame-level 与 video-level 指标。

## 5. 推荐的下一轮实验矩阵

| 实验 | 只改变的因素 | 目的 |
|---|---|---|
| A | 按视频重新划分，YOLO11n 50 epochs | 得到可信泛化基线 |
| B | A + 100/150 epochs，cosine LR | 判断是否欠拟合 |
| C | A + 水下颜色/低对比度增强 | 验证域增强收益 |
| D | A + 800 或 960 输入 | 验证小目标定位收益 |
| E | A + YOLO11s | 比较精度、速度、显存代价 |

每次实验都保存 `args.yaml`、`results.csv`、混淆矩阵、PR 曲线和 test 结果。最终选型建议以“按视频隔离测试集的 mAP@50:95 + 漏检率 + 单帧延迟”共同决定，而不是只选 mAP@50 最高的模型。

## 6. 复评命令

```powershell
# 验证集
yolo detect val model=runs/detect/underwater_baseline_v1/weights/best.pt `
  data=dataset/yolo_dataset/data.yaml split=val imgsz=640 batch=16 device=0 workers=0 plots=True

# 测试集：只在模型和阈值确定后运行
yolo detect val model=runs/detect/underwater_baseline_v1/weights/best.pt `
  data=dataset/yolo_dataset/data.yaml split=test imgsz=640 batch=16 device=0 workers=0 plots=True
```

Windows 首次扫描测试标签时，如果出现 `WinError 5`，应在有权限的终端运行一次上述命令生成 `labels/test.cache`；这不是模型错误。
