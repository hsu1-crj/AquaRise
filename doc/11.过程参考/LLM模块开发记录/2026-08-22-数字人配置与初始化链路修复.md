# LLM / 数字人模块 2026-08-22 工作记录

**负责人**：海瞳 LLM 组（LLM / 数字人）
**范围**：数字人运行配置、SDK 初始化链路、模型对话服务状态核验

## 一、数字人配置与初始化排查

- 核对 `src/frontend/.env` 中的 `VITE_DH_APP_ID`、`VITE_DH_APP_SECRET` 已配置，确认 Vite 构建产物能够注入前端变量。
- 修复后端只读取项目根 `.env` 导致 `/api/v1/digital-human/config` 错误返回未配置的问题；本地开发时兼容读取前端环境变量，服务端不返回 `appSecret`。
- 修复 `Assistant.tsx` 中 SDK 容器初始化阶段使用 `display:none` 的问题。现在初始化阶段保留可测量尺寸，仅通过 `visibility:hidden` 隐藏，避免 XmovAvatar 获取 `0×0` 容器。
- 重启前后端并验证数字人配置接口返回 `enabled=true`、`configured=true`。

## 二、外部依赖状态

- 浏览器和命令行均确认 Xmov SDK CDN `media.xingyun3d.com` 在当前网络环境出现 TLS 连接关闭（`ERR_CONNECTION_CLOSED`）。
- 代码保留 AquaCore 全息拟态和浏览器语音降级，不将网络失败误判为凭证缺失。
- 未将任何数字人密钥写入源码、工作记录或 Git 提交。

## 三、验证

- `npm run build`：通过。
- 登录后调用 `/api/v1/digital-human/config`：通过，返回公开配置且不返回密钥。
- Ollama `ds-ocean_mingzhe:latest`：本机可发现并可用。
