# 今日工作记录

日期：2026-08-20

## 生命图谱与手势交互

- 优化张开手掌进入物种档案、握拳返回地球的手势交互。
- 降低手势识别阈值和确认时间，提升识别灵敏度。
- 修复自动演示等待音频时卡住、无法正常进入鲸落流程的问题。
- 修复握拳被误判为 pinch 的问题。
- 修复手暂时离开摄像头画面时误触发返回地球的问题。
- 优化左右挥掌切换物种的防抖和触发逻辑。
- 进入生命图谱时自动请求摄像头权限。
- 更新手势图鉴中的动作说明。

## 搜索与筛选 UI

- 将物种搜索入口升级为声呐检索控制台风格。
- 增加检索提示、快捷键 `/`、箭头反馈和扫描信号视觉。
- 地球模式只保留居中的“检索物种”入口，不显示保护等级和生物类群分类。
- 将地球模式搜索入口上移至顶部 HUD 区域，减少对地球主体的遮挡。
- 物种档案模式保留保护等级和生物类群筛选。
- 修复地球模式与物种档案模式切换时筛选 UI 状态错误的问题。

## 知识库页面清理

- 删除前端“知识库”导航入口。
- 删除知识库页面路由校验和 `KnowledgePage` 组件。
- 清理知识库页面相关 imports 和模拟文档数据。
- 保留 Assistant 内部仍使用的知识库 API 能力。

## 分支合并与人脸功能

- 拉取并合并远程 `fenfenzhizhi` 分支。
- 解决 `UtilityPages.tsx` 和 `data/marine_now.json` 合并冲突。
- 保留知识库页面删除结果，同时合并分支新增的人脸识别录入功能。
- 合并提交：`52c4a26 Merge branch 'fenfenzhizhi' into master`。

## 验证与运行状态

- 前端 `npm run build` 构建通过。
- `git diff --check` 检查通过。
- 前端服务运行于 http://localhost:5173。
- 后端服务运行于 http://localhost:8000。
- 前后端健康检查均返回 HTTP 200。
