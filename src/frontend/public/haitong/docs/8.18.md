# 海瞳 · 8.18 今日更新记录 (2026-08-18)

> 🌟 **项目总策划**：`mingzhe` (Executive Producer & Chief Planner)  
> ⚠️ **非商业研学声明**：本项目内置背景音乐音频《跃》（演唱：薛之谦）仅供非商业性的**个人/内部学习、教学研究与学术交流**使用，音频版权归原作者及相关唱片公司所有。未经官方版权方授权许可，严禁用于任何形式的商业盈利行为或公开发布传播。

## 一、变更总览

1. **粒子地球真实感升级**：昼夜光照、太阳反光、大气辉光，视觉更接近真实行星。
2. **手势识别技术栈切换**（用户选择浏览器端路线）：YOLO26 服务端推理 → **MediaPipe Hands 浏览器端 WASM**，~30fps 本地关键点跟踪。
3. **项目全面清理**：删除 YOLO 模型、缓存、调试夹具与无引用旧图，活跃代码 YOLO 残留归零。

## 二、粒子地球真实感升级

- **昼夜光照**：世界固定侧方光源逐粒子点积，晨昏线垂直切分正面球体（受光侧明亮、背光侧沉入夜色），夜侧粒子颜色与透明度双重衰减；夜晚保留生物荧光底光（"海瞳"视觉身份不变）；
- **太阳反光**：受光侧粒子 `pow(·, 24)` 镜面高光，海洋泛起阳光洒落的反光；
- **大气辉光**：球体边缘 Fresnel 蔚蓝大气散射，新增 `#atmosphereGlow` 软辉光层（planet 显示 / species 隐藏）；
- 保留图层：720×360 世界陆地掩码、赤道/回归线/经线网格、极光环带、全球板块构造线、物种生态哨站。

## 三、手势识别切换 MediaPipe Hands（浏览器端 WASM）

- 引入 `@mediapipe/tasks-vision@1.0.1`（`HandLandmarker`，GPU 优先、CPU 回退），摄像头画面不再逐帧上传，浏览器内 ~30fps 本地跟踪 21 个手部关键点；
- 新增 `assets/js/hand-gestures.mjs`：纯数学的 21 关键点 → 12 类手势分类器（四指"指尖-腕距/指根-腕距"判定伸直，拇指竖起/横握/成环判定 like/grip/ok，卷曲紧度区分 fist/grip）；阈值 `HAND_RULES` 可在控制台 `window.__HAND.rules` 实时调参；
- 资源离线化：`assets/vendor/mediapipe/` 内置 `vision_bundle.mjs`、WASM 与 `hand_landmarker.task`（Google 官方，Apache-2.0），全部本地加载、0 外网请求；
- 前端复用既有管线零回归：检测框映射 320×240 坐标，投票防抖（620ms/3 帧）、palm 0.7s 计时、grip 旋转、挥手切换全部原样工作；
- 服务端瘦身：`gesture_server.py` 移除 torch/ultralytics 与模型加载（启动不再加载 5MB 模型、不再依赖 CUDA），仅保留静态托管 + `/api/health` + `/api/catalog`；旧 `POST /api/gesture` 返回 410 提示迁移；
- `start.bat`：移除模型文件检查与 CUDA 说明，改为检查 `assets/vendor/mediapipe/hand_landmarker.task`；依赖降为 `fastapi` + `uvicorn`。

## 四、项目清理

- 删除：`models/`（YOLO 模型 5.1MB）、`__pycache__/`、`t3.bat` / `t4.bat`（行尾调试遗留）、`tests/_accept-earth.png`（验收产物）、`assets/img/` 根目录 8 张无引用旧图（dugong / whaleshark / hawksbill / turtle / baiji / coral / whale / abyss，正式图在 `assets/img/species/`）；
- 文档整理：8.16/8.17 日志移入 `docs/`，README 结构树同步（移除 models/、补 docs/）；`docs/8.17.md` 中"模型可删除"说明更新为"已删除"；
- 活跃代码 grep `yolo|ultralytics|torch`：0 残留（`gesture_server.py` docstring 保留一行迁移说明）。

## 五、验证

- 冒烟测试 `tests/frontend-smoke.html`：**PASS**（含 MediaPipe 离线加载断言：`__HAND.init()` 成功、WASM/模型来自本地、0 外网请求）；
- 单元测试：`node tests/hand-gestures.test.mjs` **14/14 PASS**（合成 12 类手势关键点逐一断言）；`node tests/poster-utils.test.mjs` PASS；
- 像素级验收：陆地/海洋像素清晰可辨；上亮下暗、右亮左暗的昼夜梯度成立；辉光 planet=1 / species=0 生命周期正确；
- MediaPipe 真实浏览器加载：`vision_wasm_internal.wasm` + `hand_landmarker.task` 来自本地，外网请求 0，控制台 0 错误；
- 服务端重启即时就绪，`/api/health`、`/api/catalog`、`/api/gesture(410)` 均按预期响应。

> 遗留事项：fist/grip/like 等近似手势的关键点规则阈值需在真实摄像头与光照下按现场手感微调（`window.__HAND.last.metrics` 查看指标、`window.__HAND.rules` 调参，改完即时生效）。
